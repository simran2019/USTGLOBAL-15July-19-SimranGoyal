let empJSON = {
    name:'simran',
    age:22,
    
}
             ; // known as string interpolation
            
console.log(`my name is ${empJSON.name}`);
console.log(empJSON);
let jsonObject = JSON.stringify(empJSON);
console.log(jsonObject);
let jsObject= JSON.par(jsonObject);
console.log(jsObject);
